// Get a Client ID at https://developer.spotify.com/dashboard
// Redirect URI (Settings): must use 127.0.0.1, not "localhost" (Spotify rule).
// Examples: https://127.0.0.1:8443/  or  http://127.0.0.1:8080/
// Must match scheme, host, port, and path (add /index.html only if you use it).
window.SPOTIFY_CONFIG = {
  clientId: "92522f22850b45bda0d6286ca536444e",
};
